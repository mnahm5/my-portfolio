import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'projects',
  templateUrl: `projects.component.html`,
})
export class ProjectsComponent  { name = 'Angular'; }
